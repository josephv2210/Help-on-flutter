import 'package:flutter/material.dart';

class PageCreate extends StatelessWidget {
  const PageCreate({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Page Create", style: TextStyle(fontSize: 30),),
    );
  }
}